# Experiment: T = -15 C   (generated by preprocess/generate_study_opts.py)
#
# Kinetics are temperature-specific. The solver rescales both of these by
# rho_vs(T)/rho_ice at runtime, but beta_HK's 1/sqrt(T) and d0's 1/T are baked
# in here -- do NOT reuse this file at another temperature.
#
#   alpha_c   = 1.3410e-02   (condensation coefficient; the one free parameter)
#   rho_vs    = 1.3593e-03 kg/m^3
#   rho_vs/rho_ice = 1.4792e-06

-temp -15
-humidity 1
-t_final 2.4192e+06                  # 28 days

-beta_sub0 3.661059e+05       # K&P beta0 = beta_HK/(rho_vs/rho_i), UNSCALED
-d0_sub0 9.969300e-10         # physical gamma*V_m/(R*T), K&P Eq. 13 (NOT pre-scaled)

# Interface-CFL limiter: cap dt so no Gauss point moves more than dphimax per step
-dtCFL 1
-dtCFL_dphimax 0.2
-dtmax 200
