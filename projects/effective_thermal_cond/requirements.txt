# ---------------------------------------------------------------------------
# Python 3.13  (built and verified against CPython 3.13.7, Homebrew python@3.13)
#
#   Floor is Python 3.12: numpy >= 2.5 and scipy >= 1.18 declare
#   requires-python >= 3.12, so the previous 3.9 environment was stuck on
#   numpy 2.0.2 / scipy 1.13.1. Kept in step with enceladus_DSM and
#   lunar_regolith_DSM so all three projects share one interpreter.
#
# Recreate this environment from scratch:
#   rm -rf venv_EffK
#   /opt/homebrew/bin/python3.13 -m venv venv_EffK
#   venv_EffK/bin/python -m pip install --upgrade pip setuptools wheel
#   venv_EffK/bin/pip install -r requirements.txt
#
# Only direct dependencies are pinned; transitive versions are resolved by pip
# and constrained by these pins.
# ---------------------------------------------------------------------------

# --- core numerics / plotting ------------------------------------------------
numpy==2.5.2
scipy==1.18.1
matplotlib==3.11.1
pandas==3.0.5
cmocean==4.0.3

# --- notebooks (notebooks/fft_check.ipynb) -----------------------------------
jupyterlab==4.6.3
ipykernel==7.3.0

# NOTE: igakit is NOT required here -- nothing in this project imports it
# (it was installed in the old venv by accident).
#
# NOTE: archive/scratch/fenicsx_demo.py imports dolfinx/ufl/petsc4py/mpi4py.
# Those are not pip-installable; they live in the separate conda env behind
# .venv (miniforge "fenicsx", Python 3.11) and are intentionally not listed.
